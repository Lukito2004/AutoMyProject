using System;
using System.Collections.Generic;
using System.Text;

namespace AutoMy.DomainModels
{
    public enum AccountType { Normal, Admin }
    public enum FuelType { ბენზინი, გაზი, ელექტროენერგია, ჰიბრიდი }
    public enum WheelType { მარცხენა, მარჯვენა }
    public enum BuyType { იყიდება, ქირავდება }
    public enum LeadingWheels { წინა, უკანა }
    public enum GearBoxType { ავტომატიკა, მექანიკა }
}
