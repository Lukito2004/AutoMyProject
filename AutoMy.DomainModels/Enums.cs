using System;
using System.Collections.Generic;
using System.Text;

namespace AutoMy.DomainModels
{
    public enum AccountType { Normal, Admin }
    public enum FuelType { Petrol, Gas }
    public enum WheelType { Left, Right }
}
